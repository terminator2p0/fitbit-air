rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The dashboard uses the server-side Admin SDK. Browser access stays closed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
